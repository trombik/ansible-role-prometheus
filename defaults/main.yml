prometheus_user: "{{ __prometheus_user }}"
prometheus_group: "{{ __prometheus_group }}"
prometheus_log_dir: /var/log/prometheus
prometheus_db_dir: "{{ __prometheus_db_dir }}"
prometheus_package: "{{ __prometheus_package }}"
prometheus_service: prometheus
prometheus_conf_dir: "{{ __prometheus_conf_dir }}"
prometheus_conf_file: "{{ prometheus_conf_dir }}/prometheus.yml"
prometheus_flags: ""
prometheus_extra_packages: []

prometheus_config: ""
